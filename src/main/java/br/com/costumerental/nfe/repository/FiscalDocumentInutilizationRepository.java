package br.com.costumerental.nfe.repository;

import br.com.costumerental.nfe.domain.FiscalDocumentInutilization;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FiscalDocumentInutilizationRepository extends JpaRepository<FiscalDocumentInutilization, Long> {
}
